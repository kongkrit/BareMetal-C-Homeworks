Create ./index.html + styles.css + index.js with the following structure:

./index.html + styles.css + index.js with links to

In index.html, show links to 

./homeworkNN/index.html for every NN where ./homeworkNN/ folder exists.

Call this link "homeworkNN"

- dark gray style for every page

- Title "BareMetal-C Homeworks"
